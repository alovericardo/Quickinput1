name: QuickInput-CI
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_windows:
    runs-on: windows-latest
    env:
      QT_VERSION: "6.8.0"
      QT_ARCH: "win64_msvc2022_64"
    steps:
      - name: 拉取源码
        uses: actions/checkout@v4

      - name: 加载MSVC编译环境
        uses: ilammy/msvc-dev-cmd@v1

      - name: 配置Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 安装aqtinstall
        run: |
          python -m pip install --upgrade pip
          python -m pip install aqtinstall==3.3.* --default-timeout=120

      - name: 安装Qt6
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.0'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2022_64'

      # ========== 使用MSBuild编译VS解决方案，删除全部qmake/nmake ==========
      - name: MSBuild编译Release x64
        run: msbuild QuickInput.sln /p:Configuration=Release /p:Platform=x64

      # 编译输出位置 x64\Release
      - name: windeployqt拷贝Qt依赖dll
        run: |
          cd x64/Release
          windeployqt *.exe

      - name: 上传编译成品
        uses: actions/upload-artifact@v4
        with:
          name: QuickInput-Win-x64
          path: x64/Release/**/*
